[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
addopts = --tb=short -q
markers =
    integration: marks tests as integration tests (may require running services)
    slow: marks tests as slow (live LLM/API calls)
